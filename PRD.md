# PRD: 다달이 (Dadari)

## 0. 문서 정보
- 작성일: 2026.08.16 (최종 수정: 디자인 최종 확정)
- 작성자: (본인)
- 상태: 기획·디자인 완료, 개발 착수 단계

## 1. 배경 및 목적

온라인 MD로 재직하면서 iOS 개발자로 이직을 준비하고 있고, 이 프로젝트는 그 과정에서 나오는 포트폴리오용 개인 프로젝트다. 2026년 2월 퇴사 이후 이력서에 생긴 공백을 채우는 목적도 있지만, 그보다는 기존 경력에서 보여줬던 강점(동시성 처리, Protocol 기반 DI 테스트, 구조 설계)에 WidgetKit, App Intents, SwiftData 같은 새 기술 스택을 자연스럽게 이어붙이는 게 핵심이다.

시중 생리앱들의 UX가 불편하다고 느껴서 직접 더 잘 만들어보고 싶다는 동기도 있다. 특히 기록하는 순간의 마찰을 줄이는 데 집중한다.

## 2. 타겟 사용자

생리주기를 기록하고 싶지만 기존 앱들의 과도하게 여성적인 톤(핑크, 꽃, 이모지 범벅)이 부담스러운 사용자. 앱을 열지 않고 최대한 빠르게 기록하고 싶은 사용자. 잠금화면에서 바로 시작일/종료일을 입력할 수 있다는 점이 핵심 차별점이라 이 워크플로우 자체가 타겟을 정의한다고 봐도 된다.

## 3. 컨셉 / 디자인 원칙

생리앱이라는 티가 나지 않는 게 우선순위다. 핑크나 레드 계열, 꽃무늬 같은 전형적인 모티프는 배제한다.

### 3.1 앱 이름

**다달이 (영문 표기: Dadari)**. "매달, 다달이"라는 순우리말에서 따왔고, 로마자 표기법상 정확한 발음은 [다다리]라 Dadari로 표기한다. 뜻을 몰라도 그 자체로 브랜드 단어처럼 들리고, 앱스토어 영문 리스팅에도 그대로 쓸 수 있다.

### 3.2 UI 방향 (최종 확정)

여러 라운드의 시안을 거쳐 아래로 확정한다. 그라데이션, 유리효과(글래스모피즘), 뉴모피즘, 3색 이상의 파스텔 배색, 일반적인 도넛형 링 프로그레스는 전부 제외했다. 흔한 패턴이거나 가독성을 해치기 때문이다.

- **홈 상단**: 주간 캘린더 스트립. 평소엔 이번 주 7일만 보여주고, 탭하면 월간 그리드로 펼쳐진다. 월간에서 날짜(특히 생리 시작일)를 탭하면 그 날짜를 시작점으로 하는 새 일주일로 다시 접힌다. 애플 캘린더/건강 앱과 유사한 패턴이라 학습 비용이 낮다.
- **홈 히어로**: 도넛형 링 대신, 달이 차고 기우는 모양(문페이즈)으로 주기를 표현한다. 생리 시작(그믐) → 배란기(보름) → 다시 그믐으로 돌아가는 흐름이 실제 달의 위상과 겹쳐서, 장식이 아니라 의미 있는 모티프가 된다. 원 두 개를 겹쳐 그림자를 오프셋하는 순수 벡터 방식이라 이미지 에셋이 필요 없고, SwiftUI에서도 `ZStack` + `Circle().offset()` + `.clipShape(Circle())`로 그대로 구현하고 애니메이션까지 자연스럽게 붙는다.
- **문페이즈 단계 설명**: 생리/난포기/가임기/황체기/PMS 5단계 설명은 상시 노출하지 않고, 히어로 옆의 "?" 버튼을 탭했을 때만 팝오버로 띄운다. 평소 화면은 최대한 단순하게 유지하기 위함이다.
- **컬러 팝 카드**: 화면 전체를 색으로 덮지 않고, "오늘 기록하기" 카드 하나에만 포인트 컬러를 진하게 채워 시선을 모은다.
- **라인아트 스탯**: 평균 주기·생리 기간·예측 신뢰도 같은 보조 정보는 직접 그린 얇은 선 아이콘 + 텍스트로, 화려하지 않게 구성한다.
- **워드마크**: "다달이"에 세리프(고운바탕)를 써서 시스템 폰트만으로는 나오지 않는 브랜드 톤을 더한다.

### 3.3 색감 (최종 확정)

플랫 컬러 두 가지만 쓴다(듀오톤). 그라데이션 없이 각 색을 단색으로만 사용한다.

| 용도 | 색 |
|---|---|
| 배경 | #FFFFFF |
| 카드 | #F7F6FB |
| 주 포인트 (버튼, 문페이즈, 워드마크) | #7B82D9 (페리윙클 블루) |
| 주 포인트 텍스트용 | #5A61B8 |
| 주 포인트 옅은 배경용 | #EDEEFB |
| 보조 포인트 (두 번째 액션, 소소한 강조) | #FF9F73 (코럴) |
| 보조 포인트 텍스트용 | #E67E4F |
| 보조 포인트 옅은 배경용 | #FFEFE6 |
| 본문 텍스트 | #1C1C1E |
| 보조 텍스트 | #8A8A93 |

### 3.4 타이포그래피 (확정)

- 본문/UI: Pretendard (시스템 폰트 -apple-system 대체, 실제 iOS 빌드에서는 SF Pro 사용)
- 워드마크 및 큰 숫자: 고운바탕(Gowun Batang) — 한글 세리프, 무료(OFL 라이선스)

### 3.5 화면 흐름 및 인터랙션 노트

- 홈 화면은 위에서부터 주간 스트립 → 문페이즈 히어로 → 오늘 기록하기 카드 → 라인아트 스탯 3개 → 최근 기록 리스트 순으로 쌓는다.
- 캘린더는 별도 탭이 아니라 홈 상단에 통합돼 있고, 주간↔월간 전환이 핵심 인터랙션이다.
- 증상/컨디션 기록은 별도 화면이 아니라 바텀시트로 뜬다.
- 이 문서(3번)와 UX-설계.md, ui-mockup.html, flow-diagram.html이 서로 같은 최종 디자인을 가리키도록 맞춰져 있다. 기획·디자인 단계는 이 버전으로 종료하고, 다음 단계부터는 10번 일정에 따라 실제 구현에 들어간다.

## 4. 핵심 기능

### 4.1 Phase 1 (MVP)
| 기능 | 설명 |
|---|---|
| 주기 기록 | 생리 시작일, 종료일, 생리량(경/중/다량) 입력 및 저장 |
| 예측 | 평균 주기 기반 다음 생리일, 배란일 계산 |
| 로컬 알림 | 예정일 D-3, D-1 알림 |
| 잠금화면 위젯 | WidgetKit의 Button(intent:)에 App Intent를 직접 연결하는 인터랙티브 위젯. Shortcuts 앱을 거치지 않고 위젯 익스텐션 프로세스 안에서 바로 기록됨. 오늘 날짜는 즉시 기록, 다른 날짜로 기록할 때만 앱이 열리고 캘린더 피커로 전환 |
| 기록 수정/삭제 | 캘린더에서 날짜를 탭하면 그날 기록을 수정하거나 삭제할 수 있음. 잠금화면이 주 입력 경로라 오탭 가능성이 높아, 되돌릴 수 있는 경로가 반드시 필요함 |
| 예측 안전장치 | 예정일이 한참 지나도 새 기록이 없으면 D-day 카운트다운을 멈추고 "혹시 다른 이유가 있나요?" 정도의 안내로 전환. 임신 등 예측이 맞지 않는 상황에서 계속 틀린 숫자를 보여주지 않기 위한 최소한의 처리 |
| HealthKit 쓰기 | 기록 데이터를 menstrualFlow 카테고리로 HealthKit에 저장 (읽기는 Phase 2). 실기기 테스트 필수, 시뮬레이터 미지원 |
| CloudKit 대응 데이터 모델 | 지금은 CloudKit을 켜지 않지만, 모든 필드를 옵셔널/기본값 구조로 설계해서 v1.1에서 마이그레이션 없이 동기화를 추가할 수 있게 함 |

일정이 늦어지더라도 잠금화면 위젯은 Phase 1에 포함하기로 이미 결정된 사항이다.

### 4.2 Phase 2
- 증상, 컨디션 기록과 데이터 시각화
- HealthKit에서 다른 건강 지표를 읽어와 연동 분석
- 시중 앱 벤치마킹을 반영한 UX 고도화

## 5. 화면별 요구사항

### 5.1 잠금화면 위젯
사실상 이 앱의 메인 UX다. 원형 링이나 미니멀 카드 형태로 "D+n일"을 표시하고, 시작일 기록 버튼과 종료일 기록 버튼을 탭하면 앱을 열지 않고 오늘 날짜로 바로 기록된다. 다른 날짜를 지정하고 싶을 때만 앱이 열린다.

### 5.2 홈 화면
잠금화면과 동일한 톤의 원형/링 비주얼을 메인으로 쓰고, 최근 기록 히스토리를 아래에 리스트로 보여준다.

### 5.3 캘린더 뷰
월 단위로 실제 기록된 날짜와 예측 주기를 시각적으로 구분해서 표시한다. 기록이 있는 날짜를 탭하면 그 기록(시작일/종료일/생리량/증상)을 수정하거나 삭제할 수 있는 화면으로 진입한다.

### 5.4 설정
평균 주기 수동 조정, 알림 on/off 및 시점 설정, HealthKit 연동 토글을 제공한다.

## 6. 데이터 모델 (초안)

기록 1건에 저장할 항목은 아직 확정 전이지만 현재 기준으로는 이렇다.

| 필드 | 타입 | 비고 |
|---|---|---|
| id | UUID | |
| startDate | Date | 필수 |
| endDate | Date? | 종료 전까지는 nil |
| flow | enum (light / medium / heavy)? | 생리량. HealthKit의 HKCategoryValueVaginalBleeding과 매핑 가능 |
| symptomTags | [String]? | Phase 2 |
| source | enum (lockScreen / app) | 잠금화면 기록인지 앱 내 기록인지 구분, 추후 분석에 활용 가능 |

App Group으로 위젯과 앱이 이 데이터를 공유하고, SwiftData(또는 Core Data)로 영속화한다.

키, 몸무게는 데이터 모델에 넣지 않는다. 예측 로직이 순전히 기록 날짜 기반이라 계산에 쓰이지 않고, BMI 기반 건강 코칭 같은 기능은 스코프 밖이라 온보딩만 길어지는 결과가 된다. 나중에 HealthKit 체중 데이터와 주기를 교차 분석하는 기능을 추가하고 싶어지면 그때 v2 이후 선택 입력으로 검토한다.

### 6.1 온보딩 시 수집하는 예측 근거 데이터

마지막 생리 시작일 하나만으로는 두 번째 실제 기록이 쌓이기 전까지 예측이 기본값(28일) 추정에 머무른다. 아래 항목을 온보딩에서 함께 물어봐서 첫 예측부터 근거를 갖게 한다.

| 필드 | 타입 | 비고 |
|---|---|---|
| lastPeriodStartDate | Date | 필수 |
| estimatedCycleLength | Int? | 기본값 28. 사용자가 대략적인 범위만 알아도 선택 가능하게 구성 |
| estimatedPeriodLength | Int? | 기본값 5 |

### 6.2 예측(CyclePredictionService) 로직 원칙

애플 건강 앱의 예측 정확도는 알고리즘이 아니라 Apple Watch의 손목 체온 센서로 배란을 실측하는 하드웨어에서 나온다. 이 앱은 날짜 기록만으로 예측해야 하므로, 목표를 "애플만큼 정확하게"가 아니라 "날짜 기록만으로 낼 수 있는 최선"으로 잡는다.

- **다음 생리 예정일**: 최근 3~6회 주기 길이의 중앙값(median)을 사용한다. 평균 대신 중앙값을 쓰는 이유는 한두 번의 이상치에 덜 끌려가기 때문이다. 21일 미만, 45일 초과 주기는 이상치로 보고 계산에서 제외한다.
  - *대안 메모*: 기록이 3건뿐일 때 중앙값은 사실상 가운데 값 하나만 쓰고 나머지 둘을 버리는 셈이라, 데이터가 적을 때는 다소 거칠다. 기록이 6회 이상 쌓이면 **절사평균(trimmed mean)**, 예를 들어 최댓값 1개·최솟값 1개를 제외한 나머지의 평균으로 전환하는 것도 고려할 만하다. 이상치는 걸러내면서도 중앙값보다 더 많은 데이터를 반영한다는 장점이 있다. 지금은 구현 단순성을 위해 중앙값으로 확정하고, 실제 사용 데이터가 쌓인 뒤 필요하면 이 대안으로 교체를 검토한다.
- **배란 예상일**: 단순히 "주기 길이의 절반"으로 계산하지 않는다. 배란 이후 다음 생리까지의 황체기는 사람마다 12~14일로 비교적 일정한 편이므로, `배란 예상일 = 다음 예정일 − 14일`로 역산한다. 이 방식이 불규칙한 주기에서도 더 안정적이다.
- **가임기**: 정자 생존 기간(약 5일)과 난자 생존 기간(약 1일)을 감안해 `배란 예상일 −5일 ~ +1일`로 설정한다.
- **기록이 2건 미만일 때**: 6.1의 estimatedCycleLength를 사용한다. 2건 이상부터 실제 기록 간격으로 전환한다.
- **신뢰도 표시**: 최근 주기 편차가 크면(표준편차가 일정 기준 이상이면) D-day를 단일 숫자로 확정적으로 보여주지 않고 낮은 신뢰도 상태로 전환한다.
- **예측 중단 안전장치**: 예정일이 지나고 7일 이상 새 기록이 없으면 D-day 카운트다운을 멈추고, 예측이 빗나갔을 수 있다는 안내로 전환한다. 임신 등 예측이 맞지 않는 상황에서 계속 틀린 숫자를 보여주지 않기 위함이다.
- **v2 이후**: HealthKit 읽기 연동 시, `appleSleepingWristTemperature`(Apple Watch Series 8+ 필요) 또는 `basalBodyTemperature` 원시 체온 데이터를 가져올 수 있다. 다만 애플이 내부적으로 계산한 배란 추정치 자체는 HealthKit으로 노출되지 않고 순수 온도 수치만 제공되므로, 배란 감지 로직(기준선 대비 0.3~0.5도 상승이 3일 연속 이어지면 그 시점을 배란일로 역추정하는 자연주기법 규칙)은 직접 구현해야 한다. Apple Watch가 없는 사용자에게는 이 데이터 자체가 없으므로, 전체 사용자 대상 개선이 아니라 워치 보유 사용자 한정 보너스 기능으로 설계한다. 개인화 예측 고도화(가벼운 학습형 모델) 방향은 12번 로드맵 참고.

### 6.3 데이터 보존 전략

서버나 계정 시스템은 만들지 않는다 (8번 Out of Scope 참고). 대신 아래 원칙으로 데이터 유실 위험을 낮춘다.

- MVP 단계에서는 로컬 SwiftData가 유일한 저장소이고, iOS 기기 백업(iCloud 백업)에 자동으로 포함되도록 백업 제외 옵션을 걸지 않는다.
- HealthKit 쓰기가 사실상 이중 보험 역할을 한다. 앱 DB가 손상되어도 HealthKit 쪽에 기록이 남는다.
- 처음부터 모든 필드를 옵셔널/기본값 구조로 설계해서, 나중에 CloudKit 프라이빗 동기화(v1.1)를 붙일 때 스키마 마이그레이션이 필요 없게 한다. 이 원칙이 지켜지면 `ModelConfiguration(cloudKitDatabase:)` 한 줄만 추가하는 수준으로 동기화를 켤 수 있다.
- CloudKit을 실제로 켜기 전에는 반드시 더미 데이터로 먼저 검증하고, 로컬 데이터를 JSON으로 내보내는 안전망 기능을 먼저 만들어 둔다.

## 7. 기술 스택

- SwiftUI, WidgetKit, App Intents (iOS 17+)
- App Group + Shared Container
- SwiftData
- HealthKit
- 예측 로직은 `CyclePredictionService`로 분리해서 XCTest로 케이스별 검증
- 알림 스케줄링도 별도 서비스로 분리

### 7.1 로컬 알림 구성

서버가 없는 구조이므로 원격 알림(APNs)이 아니라 `UNUserNotificationCenter` 기반 로컬 알림만 사용한다. 서버 운영비, 푸시 대행 서비스 비용이 전혀 들지 않는다.

- **권한 요청**: 온보딩 이후 프리퍼미션 화면에서 이유를 설명한 뒤 `requestAuthorization(options: [.alert, .sound, .badge])`를 호출한다.
- **예약 시점**: 기록이 저장되거나 예측이 다시 계산될 때마다 `UNCalendarNotificationTrigger`로 예정일 D-3, D-1 알림을 예약한다.
- **탭 시 화면 이동**: 알림 콘텐츠의 `userInfo`에 이동할 화면 정보(예: `["route": "home"]`)를 담아두고, `UNUserNotificationCenterDelegate`의 `didReceive response:` 메서드에서 이 값을 읽어 앱의 네비게이션 상태를 변경한다. SwiftUI가 그 상태를 관찰하고 있다가 해당 화면(필요하면 컨디션 기록 바텀시트까지)으로 바로 이동시킨다.
- **재예약 처리**: 예측은 기록이 바뀔 때마다 달라지므로, 새 기록 저장 시 기존에 예약된 알림(`period-reminder-3d`, `period-reminder-1d`)을 `removePendingNotificationRequests(withIdentifiers:)`로 먼저 취소하고 새로 계산된 날짜로 다시 예약한다. 그렇지 않으면 날짜가 틀린 알림이 남아있게 된다.

### 7.2 CI/CD

GitHub Actions를 사용한다. Xcode Cloud보다 설정 손이 더 가지만, 워크플로우 파일이 레포에 코드로 남아 포트폴리오에서 직접 보여줄 수 있다는 이유로 선택했다.

- **트리거**: `push`(main, develop)와 `pull_request`(main 대상) 둘 다에서 실행한다. PR 시점에 검사해야 문제 있는 코드가 main에 들어가는 걸 막을 수 있고, 머지 시점에도 한 번 더 돌아 최종 안전망 역할을 한다.
- **실행 내용**: 앱 타겟과 위젯 익스텐션 타겟 둘 다 빌드 확인, `CyclePredictionService`의 XCTest 자동 실행.
- **작업 흐름**: 1인 프로젝트라도 feature 브랜치 → PR → main 머지 흐름을 쓴다. 실무 협업 프로세스를 미리 체화하는 목적도 있다.
- **범위에 포함하지 않는 것**: TestFlight 자동 배포(fastlane 등)는 코드 서명 인증서 관리까지 얽혀 복잡도가 크게 올라가므로 MVP 범위에서는 제외하고, 필요해지면 이후에 별도로 검토한다.

실제 워크플로우는 `.github/workflows/ci.yml`에 있다. 위 초안에서 아래 두 가지가 바뀌었다.

- **러너/Xcode**: `macos-14` + Xcode 15.4 대신 `macos-15`(기본 Xcode 16.x)를 쓴다. `Dadari.xcodeproj`가
  Xcode 16의 동기화 폴더 그룹(objectVersion 77)을 쓰기 때문에 Xcode 16 이상이 필요하고,
  워크플로우가 버전을 먼저 확인한 뒤 미달이면 명확한 메시지로 실패한다.
- **시뮬레이터 지정**: `name=iPhone 15`처럼 이름을 박아두면 러너 이미지가 갱신될 때 깨지므로,
  `xcrun simctl list devices available --json`에서 가장 최신 iOS의 iPhone을 골라 UDID로 지정한다.

빌드/테스트는 `Dadari` 스킴 하나로 돌린다. 이 스킴이 앱 타겟과 위젯 익스텐션 타겟을 함께 빌드하고
appex를 앱에 임베드하므로, 둘 중 하나라도 깨지면 CI가 실패한다.

## 8. 이번에 하지 않는 것 (Out of Scope)

- 서버 기반 계정 시스템, 클라우드 동기화는 Phase 1, 2 모두 범위 밖
- Android 버전
- 증상 기록에 기반한 AI 분석이나 예측 고도화
- 파트너/가족 공유 기능

## 9. 성공 지표

포트폴리오 목적이 크기 때문에 지표도 그에 맞춘다.

- TestFlight 배포 완료
- 잠금화면 위젯 기록 플로우가 실제 기기에서 안정적으로 동작
- 포트폴리오 문서화(Problem-Action-Result 형태) 완료
- 예측 로직 XCTest 커버리지 확보

## 10. 일정 (약 11~13주)

위젯 기술 검증이 프로젝트 전체에서 가장 리스크가 큰 부분이라, 앱 내 로직을 먼저 짜고 나중에 위젯을 붙이는 대신 위젯 기술 스파이크를 제일 먼저 하는 순서로 조정했다.

| 주차 | 내용 |
|---|---|
| 1주 | 레포 생성 및 GitHub Actions CI 워크플로우 셋업, 위젯 기술 스파이크: App Intents 버튼이 잠금화면에서 실제로 동작하는지 UI 없이 최소 프로토타입으로 먼저 검증 |
| 2~4.5주 | 데이터 모델 설계(CloudKit 제약 고려, App Group 공유 구조 포함) + 앱 내 기록/예측 로직 + XCTest + HealthKit 쓰기 연동 |
| 5~8주 | 잠금화면 위젯 정식 UI 구현 (스파이크에서 검증된 로직에 디자인 적용, 엣지 케이스 처리) |
| 9주 | 로컬 알림 |
| 10~11주 | UI 다듬기, 캘린더 뷰 |
| 12~13주 | TestFlight 배포 + 포트폴리오 문서화 |

HealthKit 쓰기는 코드량 자체보다 실기기 권한 테스트(삭제·재설치 반복)에서 시간이 붙는 편이라, 앱 내 로직 단계에 묶어서 함께 진행한다.

## 11. 열린 질문

- 기록 1건의 필드 구성을 최종 확정할 것 (증상 태그 여부 등)

## 12. 향후 업데이트 로드맵

| 버전 | 내용 | 비고 |
|---|---|---|
| v1.1 | CloudKit 프라이빗 동기화 | 모델을 미리 대비해뒀다면 큰 작업 없이 추가 가능. 켜기 전 JSON 내보내기 안전망 먼저 구현 |
| v1.2 | 가벼운 증상 태그 / 메모 | Flo, 해피문데이 수준의 무거운 증상 분석은 지양. Simpluna처럼 메모 한 줄 남기는 정도로 시작 |
| v1.3 | 캘린더 뷰 고도화, 데이터 시각화 | 실제 기록 데이터가 쌓인 뒤에 진행하는 게 의미 있음 |
| v2+ | HealthKit 읽기 연동, 다른 건강 지표와 교차 분석 | 우선순위 가장 낮음. 포트폴리오 관점에서는 v1까지만 완성도 있게 마무리해도 충분 |
| v2+ (탐색적) | 개인화 예측 고도화 | Flo처럼 수백만 사용자 데이터로 학습한 신경망은 이 프로젝트 규모에서 불가능. 대신 그 사용자 본인의 누적 기록(주기 길이, 증상, HealthKit 지표)만으로 개인 맞춤 가중치를 학습하는 가벼운 모델(예: 최근 기록에 더 큰 가중치를 두는 지수가중이동평균 등)을 6.2의 중앙값 로직 위에 얹는 걸 검토. 최소 1년 이상 기록이 쌓인 뒤에나 의미가 있어 우선순위는 낮음 |

증상 기록의 디테일이나 대규모 데이터 기반 AI 분석은 이미 Flo와 해피문데이가 압도적으로 잘하고 있는 영역이고, 이 프로젝트는 개인 사용자 한 명의 데이터만 다루는 구조라 애초에 같은 방식으로 경쟁할 수 없다. 그래서 계속 "잠금화면에서의 마찰 없는 기록"과 "생리앱 티가 안 나는 디자인" 두 가지 차별점에 집중하고, 예측 고도화는 그 다음이다.

