# 다달이 (Dadari)

생리주기 관리 iOS 앱. iOS 개발자 이직을 위한 포트폴리오 프로젝트.

## 필수로 먼저 읽을 문서

- `PRD.md` — 기능 범위, 데이터 모델, 예측 로직, 기술 스택, CI/CD, 일정
- `UX-설계.md` — 타겟 사용자, 핵심 행동, 화면 흐름, 필수 화면 목록, 예외/오류 처리, 버전별 범위

새 세션을 시작할 때마다 이 두 문서를 먼저 읽고 나서 작업을 시작한다. 두 문서에 없는 내용을 임의로 추측해서 구현하지 않는다. 애매하면 사용자에게 먼저 묻는다.

## 핵심 원칙 (모든 작업에서 최우선으로 지킬 것)

1. **핵심 행동은 하나다**: 잠금화면에서 탭 한 번으로 생리 시작일/종료일을 즉시 기록하는 것. 다른 모든 기능은 이걸 보조한다. 새 기능을 추가하기 전에 "이게 이 핵심 행동을 더 쉽게 만드는가"를 먼저 따진다.
2. **UIKit 화면 없음**: 100% SwiftUI. `AppDelegate`/`SceneDelegate`/Storyboard 없음. 로컬 알림 딜리게이트처럼 `UIApplicationDelegate`가 필요한 경우만 `@UIApplicationDelegateAdaptor`로 최소한만 얹는다.
3. **탭 우선, 텍스트 입력 최소화**: 날짜는 캘린더 탭 선택, 수치는 스테퍼/세그먼트, 증상은 칩. 자유 텍스트 입력은 최후의 수단으로만 고려한다.
4. **서버 없음**: 계정 시스템, 백엔드, 원격 알림(APNs) 전부 범위 밖. 로컬 알림(`UNUserNotificationCenter`)만 사용.
5. **SwiftData 모델은 CloudKit 대응 구조로**: 모든 필드를 옵셔널/기본값으로 설계해서, 나중에 CloudKit 동기화를 붙일 때 마이그레이션이 필요 없게 한다.
6. **생리앱 티가 안 나야 한다**: 핑크/레드/꽃무늬 배제. 지금까지 확정된 디자인 시스템(색상, 폰트, 컴포넌트 스타일)은 별도로 전달할 목업 파일을 기준으로 삼는다.

## 기술 스택

SwiftUI, WidgetKit, App Intents (iOS 17+), App Group, SwiftData, HealthKit(쓰기), GitHub Actions CI.

## 빌드 순서 (PRD 10번과 동일, 위젯을 가장 먼저 검증)

1. 레포 생성 → 최소 SwiftUI 앱 프로젝트 푸시 → GitHub Actions CI 워크플로우 추가
2. 위젯 기술 스파이크: App Intents 버튼이 잠금화면에서 실제로 동작하는지 UI 없이 최소 프로토타입으로 먼저 검증
3. 데이터 모델(CloudKit 제약 고려) + 기록/예측 로직 + XCTest + HealthKit 쓰기
4. 잠금화면 위젯 정식 UI
5. 로컬 알림
6. UI 다듬기, 캘린더 뷰
7. TestFlight 배포 + 포트폴리오 문서화

## 아직 반영 안 된 것

디자인 시스템(색상 코드, 폰트, 컴포넌트별 상세 스타일)은 최종 확정본을 별도로 전달할 예정. 그 전까지 UI 작업은 PRD 3번(컨셉/디자인 원칙)에 있는 색상표와 원칙을 기준으로 진행한다.

