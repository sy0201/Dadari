# UX 설계: 마디 (가칭)

## 1. 누가, 왜 쓰는가

시중 생리앱의 과도하게 여성적인 톤(핑크, 꽃, 이모지)이 부담스러운 사람. 그리고 무엇보다, 기록하려고 앱을 열고 여러 탭을 거치는 과정 자체가 귀찮은 사람. 이 두 번째 특성이 사실상 타겟을 정의한다. 잠금화면에서 바로 기록할 수 있다는 워크플로우 자체를 원하는 사람이 곧 타겟이다.

## 2. 무엇이 불편해서 개선하는가

기존 앱들의 문제는 두 가지로 나뉜다. 하나는 톤앤매너, 너무 여성스러운 색과 아이콘이 앱을 켤 때마다 눈에 밟힌다는 것. 다른 하나는 마찰, 생리가 시작된 그 순간에 앱을 찾아 켜고 캘린더까지 들어가 날짜를 탭해야 기록이 끝난다는 것. 이 프로젝트는 이 두 가지를 각각 디자인 원칙(뉴트럴 톤)과 기술 구조(잠금화면 위젯)로 푼다.

## 3. 사용 후 무엇이 달라지는가

- 기록까지 걸리는 시간이 "앱 찾기 → 열기 → 탭하기"에서 "잠금화면 탭 한 번"으로 줄어든다.
- 예측 덕분에 다음 생리를 미리 대비할 수 있다.
- 남이 화면을 봐도 무슨 앱인지 알아채기 어렵다.

## 4. 핵심 행동 (반드시 해야 할 단 하나)

**잠금화면에서 시작일/종료일을 즉시 기록하는 것.** 예측, 캘린더, 증상 기록, HealthKit 연동은 전부 이 핵심 행동을 보조하거나 그 결과를 더 유용하게 만드는 부가 기능이다. 이 우선순위는 화면 설계와 버전 계획 전체를 관통한다.

## 5. 시작부터 완료까지의 흐름

1. **앱 설치 후 최초 실행**: 온보딩 (앱 소개 1장 + 마지막 생리 시작일 입력, 예측을 위한 최소 데이터)
2. **권한 요청**: 알림 권한, HealthKit 권한을 시스템 다이얼로그 띄우기 전에 왜 필요한지 설명하는 프리퍼미션 화면
3. **홈 진입**: 첫 화면은 아직 기록이 없으므로 빈 상태로 시작, 잠금화면 위젯 추가를 유도
4. **일상 사용 (반복되는 핵심 루프)**: 잠금화면에서 탭 → 즉시 기록 → (선택) 홈에서 컨디션 기록 바텀시트
5. **필요할 때만 앱 진입**: 캘린더 확인, 지난 기록 조회, 설정 조정

## 6. 꼭 필요한 화면 (MVP)

| 화면 | 없으면 안 되는 이유 |
|---|---|
| 온보딩 (최초 설정) | 예측 로직을 돌리려면 최소 1건의 기준 데이터(마지막 생리 시작일)가 필요함 |
| 권한 프리퍼미션 | 시스템 권한 팝업만 띄우면 왜 필요한지 몰라 거부율이 높아짐 |
| 잠금화면 위젯 | 핵심 행동이 일어나는 화면 그 자체 |
| 홈 (상태 + 캘린더) | 예측, 기록 확인, 컨디션 기록 진입점 |
| 기록 수정/삭제 | 캘린더에서 날짜를 탭했을 때 진입. 잠금화면이 주 입력 경로라 오탭을 되돌릴 방법이 반드시 필요함 |
| 컨디션 기록 바텀시트 | MVP 범위에 포함된 증상 선택/입력 |
| 설정 | 주기 수동 조정, 알림, HealthKit 토글 |
| 빈 상태 (홈, 캘린더) | 최초 사용자가 반드시 거치는 상태 |
| 오류/예외 상태 | 권한 거부, 저장 실패 등 실제로 발생하는 상황 |

## 7. 데이터: 무엇을 입력하고 저장하는가

| 항목 | 필드 | 비고 |
|---|---|---|
| 기록 | startDate, endDate, flow, symptomTags, source | PRD 6번과 동일 |
| 온보딩 입력값 | lastPeriodStartDate, averageCycleLength(기본 28), averagePeriodLength(기본 5) | 최초 예측의 기준값 |
| 설정값 | notificationEnabled, notificationDaysBefore, healthKitSyncEnabled | |

## 8. 권한: 누가 보고 수정할 수 있는가

서버도 계정도 없는 로컬 전용 앱이라(PRD 8번 Out of Scope), 기기 소유자 본인만 보고 수정할 수 있다. 다만 민감한 건강 데이터인 만큼, 설정에 Face ID 잠금 옵션을 두는 걸 고려할 만하다. 이건 MVP 필수는 아니고 v1.1 이후 후보다.

## 9. 범위: 예외, 빈 화면, 오류

### 예외 처리
- 시작일보다 이른 종료일을 입력하려는 경우
- 미래 날짜를 기록하려는 경우
- 잠금화면에서 버튼을 빠르게 연타해 중복 기록이 발생하는 경우 (idempotent 처리)

### 빈 화면
- 최초 실행 후 기록이 하나도 없는 홈/캘린더가 어떻게 보이는지
- 이번 달에 로그된 기록이 없는 캘린더

### 오류 상태
- HealthKit 권한을 거부한 경우
- 알림 권한을 거부한 경우
- 로컬 저장 실패 (드문 케이스지만 UI가 있어야 함)

## 10. 버전별로 무엇이 필요하고 무엇이 필요 없는가

| 버전 | 필요한 것 | 필요 없는 것 |
|---|---|---|
| MVP (v1) | 6번 표의 8개 화면 전부, 로컬 저장, HealthKit 쓰기 | Face ID 잠금, CloudKit, 심화 통계, 증상 기반 인사이트 |
| v1.1 | CloudKit 동기화, (선택) Face ID 잠금 | 파트너 공유, 서버 계정 |
| v1.2 | 증상 태그 v1.1보다 확장 (경향 보여주기 정도) | AI 기반 증상 분석 |
| v1.3 | 캘린더 시각화 고도화 | |
| v2+ | HealthKit 읽기, 다른 지표 교차 분석 | 파트너 공유, AI 챗봇형 인사이트 (Flo식 접근은 계속 지양) |

이 우선순위는 4번의 핵심 행동 하나를 지키기 위한 것이다. 매 버전마다 "이게 잠금화면 기록이라는 핵심 행동을 더 쉽게 만드는가, 아니면 그냥 부가 기능을 늘리는 것인가"를 먼저 물어보고 순서를 정한다.

